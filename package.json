{
  "name": "tcms-content-schema",
  "version": "1.0.0",
  "description": "Content schemas for TCMS",
  "main": "index.js",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen:types": "ts-node ./scripts/generate-schema-types.ts ./schemas",
    "replace-cuid:playgound": "ts-node ./scripts/replace-cuid.ts ./playground",
    "replace-cuid": "ts-node ./scripts/replace-cuid.ts"
  },
  "author": "Yuri Fedorov",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.6.0",
    "cuid": "^3.0.0",
    "json-schema-to-typescript": "^15.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
